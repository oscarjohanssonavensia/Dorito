const enum Types {
    TYPE_SHIP,
    TYPE_CHARGE,
    TYPE_BULLET,

    TYPE_ENEMY,
    TYPE_ENEMY_BULLET,

    TYPE_ASTEROID,
    TYPE_ASTEROID_PART,
    TYPE_CUSTOM_PARTICLE,
    TYPE_PARTICLE,

    TYPE_MISC,
}

export default Types;